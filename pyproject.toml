[project]
name = "diffpes"
version = "2026.06.13"
description = "Differentiable ARPES simulations in JAX"
authors = [
    {name = "Debangshu Mukherjee", email = "mukherjeed@ornl.gov"},
    {name = "Jacob Cook", email = "cookjl2@ornl.gov"},
]
license = "MIT"
license-files = ["LICENSE"]
maintainers = [{name = "Debangshu Mukherjee", email = "mukherjeed@ornl.gov"}]
keywords = [
    "ARPES",
    "MBE",
    "JAX",
    "Differentiable Programming",
]
readme = "README.md"
requires-python = ">=3.12,<3.15"
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering :: Physics",
    "Topic :: Scientific/Engineering :: Chemistry",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Typing :: Typed",
]
dependencies = [
    "numpy>=2.2.1",
    "matplotlib>=3.10.0",
    "jax>=0.7.0",
    "jaxtyping>=0.3.0",
    "chex>=0.1.89",
    "beartype>=0.21.0",
    "h5py>=3.15.1",
    "equinox>=0.13.8",
    "optimistix>=0.1.0",
    "optax>=0.2.8",
    "lineax>=0.1.0",
    "ipywidgets>=8.1.0",
    "jupyter>=1.1.1",
    "ipykernel>=7.2.0",
    "nbconvert>=7.16.6",
]

[project.optional-dependencies]
cuda = [
    "jax[cuda12]>=0.7.0 ; platform_machine == 'x86_64' and sys_platform == 'linux'",
    "nvidia-cudnn-cu12>=9.5.0 ; platform_machine == 'x86_64' and sys_platform == 'linux'",
]
docs = [
    "sphinx>=7.0.0",
    "sphinx-rtd-theme>=3.0.2",
    "myst-parser>=2.0.0",
    "furo>=2025.7.19",
    "sphinx-autodoc-typehints>=3.0.1",
    "numpydoc>=1.9.0",
    "pydoclint>=0.7.3,<0.9",
    "interrogate>=1.7.0",
]
test = [
    "pytest>=8.3.5",
    "pytest-cov>=6.0.0",
    "pytest-xdist>=3.0.0",
    "chex>=0.1.89",
    "psutil>=5.9",
    "hypothesis>=6.100",
    "mpmath>=1.3.0",
    "scipy>=1.15.0",
]
dev = [
    "diffpes[docs,test]",
    "ruff==0.16.3",
    "pygount>=3.1.0",
    "pre-commit>=4.5.1",
    "ty>=0.0.19",
]
dev_cuda = [
    "diffpes[dev,cuda]",
]
all = [
    "diffpes[docs,test,dev,cuda]",
]

[build-system]
requires = ["uv_build>=0.10.4,<0.13.0"]
build-backend = "uv_build"

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple/"
publish-url = "https://upload.pypi.org/legacy/"
explicit = true

[tool.pytest.ini_options]
addopts = "-n auto --dist loadgroup --jaxtyping-packages=diffpes,beartype.beartype"
testpaths = ["tests"]
python_files = "test_*.py"
python_classes = "Test*"
python_functions = "test_*"
markers = [
    "slow: long-running reference tests; excluded per push, run on the schedule",
    "big_mem: serialize memory-intensive tests on one xdist worker",
    "rss_limit_mb(limit): override the per-test RSS growth limit in MiB",
]

[tool.coverage.run]
source = ["diffpes"]
omit = ["*/tests/*", "*/site-packages/*"]

[tool.pydoclint]
style = "numpy"
check-return-types = false
check-yield-types = true
check-arg-order = true
require-return-section-when-returning = true
require-yield-section-when-yielding = true
native-mode-noqa-location = "definition"

[tool.interrogate]
fail-under = 90
exclude = ["tests", "build", "dist"]
ignore-init-module = true
quiet = true

[tool.ruff]
include = [
    ".github/**/*.py",
    "automatons/**/*.py",
    "docs/**/*.py",
    "pyproject.toml",
    "scripts/**/*.py",
    "src/**/*.py",
    "tests/**/*.py",
]
line-length = 79
target-version = "py312"
exclude = [
    ".bzr",
    ".direnv",
    ".eggs",
    ".git",
    ".git-rewrite",
    ".hg",
    ".ipynb_checkpoints",
    ".mypy_cache",
    ".nox",
    ".pants.d",
    ".pyenv",
    ".pytest_cache",
    ".pytype",
    ".ruff_cache",
    ".svn",
    ".tox",
    ".venv",
    ".vscode",
    "__pypackages__",
    "_build",
    "buck-out",
    "build",
    "dist",
    "node_modules",
    "site-packages",
    "venv",
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "lf"
docstring-code-format = true
docstring-code-line-length = 79

[tool.ruff.lint]
select = [
    "D",
    "E",
    "F",
    "B",
    "I",
    "N",
    "UP",
    "ANN",
    "S",
    "A",
    "C4",
    "PIE",
    "PT",
    "RET",
    "SIM",
    "SLF",
    "ARG",
    "ERA",
    "PL",
]
ignore = [
    "ANN401",
    "PYI021",
    "F722",
    "COM812",
    "D205",
    "D212",
    "UP037",
    "F821",
]

[tool.ruff.lint.flake8-bugbear]
extend-immutable-calls = [
    "jax.numpy.zeros",
    "jax.numpy.ones",
    "jax.numpy.array",
    "jax.numpy.asarray",
    "jnp.zeros",
    "jnp.ones",
    "jnp.array",
    "jnp.asarray",
]

[tool.ruff.lint.pylint]
max-args = 10

[tool.ruff.lint.isort]
known-third-party = ["jax", "jaxtyping"]

[tool.ruff.lint.pydocstyle]
convention = "numpy"

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = [
    "S101",
    "RET504",
    "PLR2004",
    "PT009",
    "ARG001",
    "E741",
    "N803",
    "N806",
]
# Tight-binding and angular-momentum formulae use standard physics symbols.
"src/diffpes/maths/*.py" = ["E741", "N803", "N806"]
"src/diffpes/tightb/*.py" = ["E741", "N803", "N806"]
[tool.ty.environment]
python-version = "3.12"
root = ["./src", "."]
# Pin the analysis platform so type-checking is identical across CI runners.
python-platform = "linux"

[tool.ty.src]
include = ["src", "tests", "docs", "automatons"]

[tool.ty.rules]
too-many-positional-arguments = "ignore"
missing-argument = "ignore"
unknown-argument = "ignore"
no-matching-overload = "ignore"
invalid-assignment = "ignore"
invalid-argument-type = "ignore"
unresolved-attribute = "error"

[[tool.ty.overrides]]
include = ["tests/**"]

[tool.ty.overrides.rules]
unresolved-attribute = "ignore"
not-subscriptable = "ignore"
unsupported-operator = "ignore"
